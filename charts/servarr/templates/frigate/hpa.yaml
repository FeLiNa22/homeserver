{{- if and .Values.frigate.enabled .Values.frigate.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Release.Name }}-frigate
  labels:
    app.kubernetes.io/name: frigate
    app.kubernetes.io/instance: {{ .Release.Name | quote }}
    app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
    app.kubernetes.io/managed-by: {{ .Release.Service | quote }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Release.Name }}-frigate
  minReplicas: {{ .Values.frigate.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.frigate.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.frigate.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.frigate.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.frigate.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.frigate.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
