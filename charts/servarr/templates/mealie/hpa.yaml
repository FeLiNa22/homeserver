{{- if and .Values.mealie.enabled .Values.mealie.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Release.Name }}-mealie
  labels:
    app.kubernetes.io/name: mealie
    app.kubernetes.io/instance: {{ .Release.Name | quote }}
    app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
    app.kubernetes.io/managed-by: {{ .Release.Service | quote }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Release.Name }}-mealie
  minReplicas: {{ .Values.mealie.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.mealie.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.mealie.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.mealie.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.mealie.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.mealie.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
