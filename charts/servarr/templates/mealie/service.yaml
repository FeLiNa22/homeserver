{{- if .Values.mealie.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-mealie
  labels:
    app.kubernetes.io/name: mealie
    app.kubernetes.io/instance: {{ .Release.Name | quote }}
    app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
    app.kubernetes.io/managed-by: {{ .Release.Service | quote }}
spec:
  type: {{ .Values.mealie.service.type | default "ClusterIP" }}
  ports:
    - port: {{ .Values.mealie.service.port | default 9000 }}
      targetPort: http
      protocol: TCP
      name: http
{{ if (and (eq .Values.mealie.service.type "NodePort") (not (empty .Values.mealie.service.nodePort))) }}
      nodePort: {{.Values.mealie.service.nodePort}}
{{ end }}
  selector:
    app.kubernetes.io/name: mealie
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
