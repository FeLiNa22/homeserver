{{- if .Values.vaultwarden.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-vaultwarden
  labels:
    app.kubernetes.io/name: vaultwarden
    app.kubernetes.io/instance: {{ .Release.Name | quote }}
    app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
    app.kubernetes.io/managed-by: {{ .Release.Service | quote }}
spec:
  type: {{ .Values.vaultwarden.service.type | default "ClusterIP" }}
  ports:
    - port: {{ .Values.vaultwarden.service.port | default 80 }}
      targetPort: http
      protocol: TCP
      name: http
{{ if (and (eq .Values.vaultwarden.service.type "NodePort") (not (empty .Values.vaultwarden.service.nodePort))) }}
      nodePort: {{.Values.vaultwarden.service.nodePort}}
{{ end }}
  selector:
    app.kubernetes.io/name: vaultwarden
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
