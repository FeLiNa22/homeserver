{{- if and .Values.immich.enabled .Values.immich.persistence.library.enabled (not .Values.immich.persistence.library.existingClaim) }}
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ .Release.Name }}-immich-library
  labels:
    app.kubernetes.io/name: immich
    app.kubernetes.io/instance: {{ .Release.Name | quote }}
    app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
    app.kubernetes.io/managed-by: {{ .Release.Service | quote }}
  {{- if .Values.immich.persistence.library.annotations }}
  annotations:
    {{- toYaml .Values.immich.persistence.library.annotations | nindent 4 }}
  {{- end }}
spec:
  accessModes:
    - {{ .Values.immich.persistence.library.accessMode | quote }}
  {{- if .Values.immich.persistence.library.storageClass }}
  storageClassName: {{ .Values.immich.persistence.library.storageClass | quote }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.immich.persistence.library.size | quote }}
{{- end }}
