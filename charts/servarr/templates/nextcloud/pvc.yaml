{{- if and .Values.nextcloud.enabled .Values.nextcloud.persistence.enabled (not .Values.nextcloud.persistence.existingClaim) }}
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ .Release.Name }}-nextcloud
  labels:
    app.kubernetes.io/name: nextcloud
    app.kubernetes.io/instance: {{ .Release.Name | quote }}
    app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
    app.kubernetes.io/managed-by: {{ .Release.Service | quote }}
  {{- if .Values.nextcloud.persistence.annotations }}
  annotations:
    {{- toYaml .Values.nextcloud.persistence.annotations | nindent 4 }}
  {{- end }}
spec:
  accessModes:
    - {{ .Values.nextcloud.persistence.accessMode | quote }}
  {{- if .Values.nextcloud.persistence.storageClass }}
  storageClassName: {{ .Values.nextcloud.persistence.storageClass | quote }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.nextcloud.persistence.size | quote }}
{{- end }}
